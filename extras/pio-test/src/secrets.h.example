#pragma once
// Copy this file to `secrets.h` (in this src/ folder; gitignored) and fill in.

#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASS "YOUR_PASSWORD"

// An event id that exists in the kit deployed to your Hapbeat device (Studio).
#define HAPBEAT_EVENT "impact.hit"
